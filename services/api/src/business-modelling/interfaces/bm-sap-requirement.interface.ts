export interface BMSAPRequirement {
  learnerId: string;
  sapUser: boolean;
  bmRequired: boolean;
  activitiesRequired: number;
  activitiesCompleted: number;
  examRequired: boolean;
  examPassed: boolean;
  certificationEligible: boolean;
}
