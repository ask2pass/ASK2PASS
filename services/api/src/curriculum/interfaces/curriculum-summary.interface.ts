export interface CurriculumSummary {
  curriculumId: string;
  programmeCategory: string;
  examination?: string;
  programme: string;
  subject: string;
  topic: string;
  lessonTitle: string;
  active: boolean;
}
